// import api from "./api";

// export const registerUser = (userData) => api.post("auth/register", userData);

// export const loginUser = (credentials) => api.post("auth/login", credentials);


// import api from "../api";

// const { data } = await api.get("/chef/profile/123"); // токен вже підставлений
